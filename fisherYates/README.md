The Fisher-Yates shuffle constructs an in-place random permutation of a vector in linear time.  

* How to run the unit tests:  
  ```bash
  g++ fisherYates.cpp -o fisherYates  
  ./fisherYates
  ```
